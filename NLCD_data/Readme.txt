Due to a maximum file size limitation on GitHub, Staff has split the NLCD data file into two parts.
Upon downloading two files, the user expected to merge them into a single file before using it for the AFC system. 
